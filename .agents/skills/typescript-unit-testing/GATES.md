## Phase 1: Structure Correct

- [ ] Tests follow the project's directory and naming conventions
- [ ] `test/` directory used (not `__test__` or `tests/`)

## Phase 2: Configuration Correct

- [ ] `vitest.config.ts` exists with `include` covering relevant paths
- [ ] `"test": "vitest run"` script in `package.json`

## Phase 3: Tests Pass

- [ ] `vitest run` passes
